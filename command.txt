
// ใช้คำสั่งนี้เพื่อตรวจสอบว่า Docker สามารถทำงานได้อย่างถูกต้องหรือไม่ โดยการรีรีนคอนเทนเนอร์ตัวอย่างชื่อว่า "hello-world"
docker run hello-world

// ใช้คำสั่งนี้เพื่อตรวจสอบเวอร์ชั่นของ Docker ที่ติดตั้งในระบบของคุณ
docker -v


// ใช้คำสั่งเพื่อตรวจสอบสถานะของคอนเทนเนอร์ทั้งหมดที่กำลังทำงานอยู่ในระบบ

// ใช้คำสั่งนี้เพือตรวจสอบการเชื่อมต่ออินเทอร์เน็ตภายในคอนเทนเนอร์ โดยการรีนคอนเทนเนอร์ที่ใช้ภาพ "busybox" และทำการ ping ไปยัง google.com
docker run busybox ping google.com

// ใช้คำสั่งนี้เพื่อหยุดคอนเทนเนอร์ที่กำลังทำงานอยู่ โดยแทนที่ <container_id_or_name> ด้วย ID หรือชื่อของคอนเทนเนอร์
docker

// ใช้คำสั่งเพื่อดึงอิมเมจจาก Docker hub โดยแทนที่ <image_name> ด้วยชื่อ

// ใช้คำสั่งนี้เพือตรวจสอบสถานะของคอนเทนเนอร์ที่กำลังทำงานอยู่ในระบบของคุณ
docker ps

// ใช้คำสั่งนี้เพื่อสร้างคอนเทนเนอร์ใหม่จากอิมเมจ "busybox" โดยไม่ต้องรีรีนคอนเทนเนอร์นั้นทันที
docker create busybox ping google.com



// ใช้คำสั่งนี้เพื่อลบคอนเทนเนอร์ที่ไม่จ้องการอีกต่อไป โดยแทนที่ <container_id_or_name> ด้วย ID หรือชื่อของคอนเทนเนอร์ที่คุณต้องการลบ
docker rm <container_id_or_name>

// ใช้คำสั่งนี้เพื่อลบคอนเทนเนอร์ที่ไม่จ้องการอีกต่อไป โดยแทนที่ <image_id_or_name> ด้วย ID หรือชื่อของคอนเทนเนอร์ที่คุณต้องการลบ
docker system prune

// ใช้คำสั่งนี้เพื่อรันคอนเทนเนอร์ที่ใช้ภาพ "nginx" ซึ่งเป็นเว็บเซิร์ฟเวอร์ยอดนิยม โดยจะทำเปิดพอร์ต 80 ของคอนเทนเนอร์ไปยังพอร์ต 8080 ของโฮสต์ของคุณ
docker run nginx

// ใช้คำสั่งนี้เพื่อ
docker exec -it <container_id_or_name> sh

// ใช้เพื่อ
docker exit -it <container_id_or_name> bash

docker pull wordpress
docker pull mysql:5.7
docker pull phpmyadmin/phpmyadmin

//
cd/user/share/nginx/html
cat index.html

docker run -d p 9000:80 nginx

